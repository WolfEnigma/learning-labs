# Day 02 — Core Git Workflow (PowerShell)

## Git Lifecycle
Working Directory → Staging Area → Repository

## Commands Practiced (PowerShell)

```powershell
git status
git add .
git commit -m "Day 02 commit using PowerShell"
git log --oneline
git diff
