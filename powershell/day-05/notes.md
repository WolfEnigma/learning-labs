# Day 05 — Git Stash (PowerShell)

## Scenario
Working on a feature but need to switch branches urgently.

## Commands Used

```powershell
git status
git stash
git stash list
git checkout main
git stash apply
